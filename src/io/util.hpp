#ifndef IO_UTIL_HPP
#define IO_UTIL_HPP

/**
 * Check whether the current process is running in interactive mode or not.
 *
 * @return True iff the current process is running in interactive mode.
 */
bool is_interactive();

#endif // IO_UTIL_HPP
